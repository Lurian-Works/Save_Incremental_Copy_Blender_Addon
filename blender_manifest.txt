schema_version = "1.0.0"

id = "save_incremental_copy"
version = "1.0.0"
name = "Save Incremental Copy"
tagline = "Save an Incremental Copy without switching files"
maintainer = "Lurian"
type = "add-on"

# # link to documentation, support, source files, etc
website = "https://github.com/Lurian-Works/Save_Incremental_Copy_Blender_Addon"

tags = ["Import-Export", "Pipeline"]

blender_version_min = "5.0.0"

# License conforming to https://spdx.org/licenses/ (use "SPDX: prefix)
https://docs.blender.org/manual/en/dev/advanced/extensions/licenses.html
license = [
  "SPDX:GPL-3.0-or-later",
]

# # Optional: add-ons can list which resources they will require:
* files (for access of any filesystem operations)

[permissions]
files = "read blend file names/save file"
